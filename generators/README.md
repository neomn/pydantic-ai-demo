# data (log, metric, trace) generator module 

    this module is meant for generating the data like logs metrics and traces 
    .py file are the generators and .txt files are used for saving those data 
    and .config file is meant for dynamically change the data generation configs like data generation rate and so on
